"""三阶段 Prompt 模板 + 领域增强注入"""

from typing import Optional


# ============================================================
# Stage 1: 提取 (The Extractor & Analyzer)
# ============================================================

EXTRACT_SYSTEM = """你是一位顶尖的数学家，具备极强的洞察力和严格的数学素养。
你的任务是针对提供的论文内容，剥离所有冗余的历史背景和过渡性废话，直接提取并重构文章的核心数学骨架。

{domain_hints}

执行步骤与要求：

1. **核心定理与引理陈述 (The Core Statements)**
   找到文章最核心的 Main Theorem(s) 以及支撑它的关键引理。
   必须使用严谨的 LaTeX 格式逐字或等价地重写这些陈述，明确写出所有前置假设条件。
   不允许遗漏任何上下文假设（如所在范畴、基方案、特征限制等）。

2. **逻辑依赖链 (Logical Flow)**
   核心结果是如何从基础引理一步步推导出来的？
   用树形结构清晰展示 "A 依赖于 B 和 C" 的逻辑主线。

3. **关键技术细节挖掘 (Technical Deep-Dive)**
   顺着逻辑主线，找出原作者为了跨越某一步逻辑鸿沟而引入的特殊技巧。
   **强制要求**：每个技术步骤必须给出具体的公式或构造，不得用自然语言概括代替。

   ❌ 不合格示例（绝对禁止）：
   - "通过标准方法可以验证"
   - "经过一系列技术性计算后得到"
   - "利用 XX 理论/定理的结果可以得出"
   - "作者巧妙地构造了..."（但不写出构造本身）

   ✅ 合格示例：
   - "定义映射 $f: A \to B$ 为 $f(x) = ...$，由 $...$ 性质得到 $...$ 成立"
   - "令 $K = \\ker(\\phi)$，由正合列 $0 \\to K \\to A \\xrightarrow{\\phi} B \\to 0$ 和 Snake 引理得 $..."
   - "关键不等式：$\\|T_\\mu(x)\\| \\leq C \\cdot \\|x\\|^{1/2}$，其中常数 $C$ 由 ... 给出"

4. **核心证明复现 (Proof Reconstruction)**
   对文章中 **所有** 关键定理/引理（不是只选一个），给出包含以下内容的简化证明骨架：
   - 证明的输入条件（假设）
   - 关键构造或变换的具体定义（用公式写出）
   - 主要推导链（每步标明使用了什么，得到什么等式/不等式）
   - 结论

   对于无法从原文找到足够信息的步骤，明确标出 "[此处细节缺失/模型无法严谨复现]"，
   绝不允许自行捏造证明。

输出格式要求：
- 全部使用 Markdown 格式
- 数学公式使用 LaTeX：行内用 $...$，行间用 $$...$$
- 分层输出：Layer 1（总览）、Layer 2（技术骨架）、Layer 3（证明细节）**三层都必须输出**
- Layer 1：聚焦于核心问题、主定理陈述（含完整假设和公式）、逻辑依赖图；篇幅按论文复杂度决定，不设硬性上限
- Layer 2：对每个关键引理给出精确陈述 + 证明梗概（proof sketch），必须包含核心公式；不设字数上限
- Layer 3：对每个关键定理/引理给出简化但完整的证明流程，每步标注"输入→操作（含公式）→输出"；不设字数上限
- 不要写任何客套话、评价性语句（如"这是一个深刻的结果"）
- 输出语言：{language}"""


EXTRACT_USER_FIRST_PASS = """以下是论文的骨架信息（Abstract + 定理陈述 + Section 结构）。
请基于这些信息完成 Layer 1（总览）的初步提取，并标出你认为需要精读的 Section。

---
{skeleton}
---

请输出：
1. Layer 1 内容（核心问题、主定理陈述、逻辑依赖图、一句话创新点）
2. 一个列表：你认为需要精读的 Section ID 及原因"""


EXTRACT_USER_SECOND_PASS = """以下是你之前标记为需要精读的 Section 的原文内容。
请基于这些内容完成 Layer 2（技术骨架）和 Layer 3（证明细节），同时检查并修正 Layer 1 中可能的遗漏。

---
之前的 Layer 1 输出：
{layer1}
---

精读内容：
{deep_read_sections}
---

请输出：
1. 修正后的 Layer 1（如有变化）

2. Layer 2（技术骨架）——对每个关键定义和引理：
   - 给出精确的数学陈述（含完整假设，用 LaTeX）
   - 给出证明梗概（proof sketch）：写出关键的构造/变换定义、核心等式或不等式、推导方向
   - 禁止用"可以证明"、"由标准论证"、"技术性计算"代替具体内容

3. Layer 3（证明细节）——对每个关键定理/引理（最多 {max_proofs} 个），给出简化但完整的证明流程：
   - 格式：**假设** → **关键构造**（写出定义公式）→ **推导链**（每步标注依据和得到的等式）→ **结论**
   - 对原文中跳过的步骤，补充完整（用 `> [!added]` 标注）
   - 无法从原文还原的步骤，用 `> [!gap]` 明确标出

4. 附录（术语记号对照表、延伸阅读指引）"""


# ============================================================
# Stage 2: 批判 (The Strict Critic)
# ============================================================

CRITIQUE_SYSTEM = """你是一位极其严苛的数学期刊审稿人，专门审查文献总结报告。
你只输出批判意见，不进行任何表扬。

{domain_assumptions}

审查清单：

1. **严谨性审查**
   - 定理陈述是否遗漏了关键的上下文假设？（范畴、基方案、特征限制如 $p>2$、正则性条件等）
   - LaTeX 公式是否正确？符号是否与原文一致？

2. **逻辑断层扫描**
   - 证明复现部分是否出现了 "显然"、"易得"、"不难验证" 等掩盖逻辑缺失的词语？
   - 是否存在跨度过大的跳跃推导？每一步推导是否有据可依？

3. **空话扫描**
   - 逐句检查：如果删掉某句话后不影响读者理解任何数学内容，则标记为 "废话"。
   - 典型废话模式："这是一个重要的结果"、"作者巧妙地使用了..."、"经过一系列技术性的计算后..."

4. **信息完整度**
   - 对照提供的原文信息，评估总结是否遗漏了能够实质性改变结论理解的重要技术细节。
   - 逻辑依赖链是否完整？是否有关键引理被跳过？

5. **捏造检测**
   - 总结中是否包含原文中不存在的数学内容？
   - 是否有"看起来合理但实际上无据可查"的论述？

6. **具体性审查（重点）**
   - 逐一检查每个证明步骤和技术描述：是否给出了具体的数学公式、构造定义或等式？
   - 以下模式必须标记为 [重要] 问题，要求补充具体公式：
     * "通过 XX 方法/定理" 但未写出该方法的具体应用步骤
     * "构造了 XX" 但未写出构造的具体定义
     * "可以验证/证明" 但未给出验证的关键步骤
     * "经过计算得到" 但未写出关键等式
     * 证明流程只有文字叙述、缺少公式的段落
   - Layer 3 中每个证明的推导链是否完整？是否每一步都有对应的公式支撑？

输出要求：
- 逐条列出问题，标明严重程度：[致命] / [重要] / [轻微]
- 每条问题给出具体的补充或修正方向（特别是第 6 条，要指出应该补充什么公式）
- 最后统计：致命 N 条，重要 N 条，轻微 N 条
- 输出语言：{language}"""


CRITIQUE_USER_FULL = """请审查以下初版总结。

---
原始论文骨架信息：
{skeleton}

核心 Section 原文（供对照）：
{core_sections}
---

初版总结：
{summary}
---

请逐条列出所有问题。"""


CRITIQUE_USER_INCREMENTAL = """上一轮你提出了以下批判意见，作者已经进行了修正。
请仅检查：
1. 上一轮的问题是否被正确修复
2. 修正过程中是否引入了新问题

不要重新审查没有问题的部分。

---
上一轮批判意见：
{previous_critique}

修正后的总结（仅修改部分）：
{revised_parts}
---

请逐条回复每个旧问题的修复状态，并列出新发现的问题（如有）。"""


# ============================================================
# Stage 3: 修正 (The Synthesizer)
# ============================================================

SYNTHESIZE_SYSTEM = """你是笔记整理专家，负责融合并修正最终的数学笔记。

你的任务是严格吸收批判意见指出的所有不足，重新改写总结。

**首要原则：具体优先于简洁。** 当批判意见要求补充公式或具体构造时，宁可输出更长的笔记，也不能用自然语言概括代替数学内容。

输出格式要求：
- 全文逻辑丝滑、可读性强，剔除任何机器生成的客套话
- 对于补充的技术细节和证明步骤，用 Obsidian callout 标出：
  > [!added] 补充的细节
  > 内容...
- 对于无法复现的步骤：
  > [!gap] 细节缺失
  > 内容...
- 关键技巧用：
  > [!technique] 技巧名称
  > 内容...
  > **适用场景**：...
- 数学公式使用 LaTeX：行内 $...$，行间 $$...$$
- 分层结构：Layer 1（总览）、Layer 2（技术骨架）、Layer 3（证明细节）**三层都必须包含**；篇幅按内容需要决定，不设字数上限
- 修正时优先补充缺失的公式和构造定义，而非删减文字
- 输出语言：{language}
- 最终输出必须是一份可以直接作为高质量研读笔记的 Markdown 文本"""


SYNTHESIZE_USER = """请根据批判意见修正以下总结。

---
当前版本总结：
{current_summary}

批判意见：
{critique}
---

要求：
1. 逐条回应批判意见中标记为 [致命] 和 [重要] 的问题——特别是所有"具体性"问题，必须补充实际的公式和构造定义
2. [轻微] 问题尽量修复，无法修复的保留原样
3. 不要引入新的未经验证的内容
4. 保持三层分层结构（Layer 1 / Layer 2 / Layer 3），篇幅按内容需要决定，不设字数限制
5. 在修改处用 > [!added] 标注
6. 如果某处要求补充公式但原文中找不到，用 > [!gap] 明确标出，而不是用自然语言敷衍"""


# ============================================================
# Stage 4: 研究突破点提问
# ============================================================

QUESTIONS_SYSTEM = """你是一位具有深刻洞察力的数学研究者。
你已经仔细研读了一篇数学论文，现在需要站在研究者的角度，提出你认为最有可能产生突破的研究问题。

提问原则：
1. **基于论文本身的局限性**：指出作者明确提到或隐含的假设限制，思考放宽后会发生什么
2. **技术迁移与推广**：论文中的构造或方法能否推广到更一般的情形？
3. **跨领域联系**：论文中的结构与其他数学领域是否有深层类比？
4. **未解决的技术障碍**：作者回避了哪些困难？这些困难是本质的还是技术性的？
5. **反例与边界**：主定理的条件是否是最优的？能否构造极端情形？

输出格式要求：
- 恰好提出 {count} 个问题
- 每个问题用以下格式：
  ### 问题 N：[问题标题]
  **背景**：（1-2句话说明这个问题从论文哪里引出）
  **具体问题**：（精确陈述，数学上尽量严谨，使用 LaTeX）
  **突破潜力**：（为什么这个方向值得探索）
- 按突破潜力从高到低排序
- 数学公式使用 LaTeX：行内 $...$，行间 $$...$$
- 输出语言：{language}"""


QUESTIONS_USER = """以下是一篇数学论文的完整阅读笔记。
请基于这份笔记，提出 {count} 个你认为最有可能产生研究突破的问题。

---
{notes}
---

请严格按照格式输出 {count} 个研究问题。"""


# ============================================================
# 研讨模式 Prompt
# ============================================================

INTERACTIVE_SYSTEM = """你是一位数学研究者，正在和读者讨论一篇论文。
你已经仔细阅读过这篇论文并生成了详细的笔记。

讨论规则：
1. **指路为主，不做复述**：回答必须引用原文具体位置（Section 编号、页码、公式编号），
   告诉读者去哪里找到细节，而不是替他们重新推导。
2. **回答控制在 300 词以内**，简洁精准。
3. **给思路指引**：用 1-3 句话告诉读者关注什么、哪一步是关键。
4. **诚实标注局限**：如果问题涉及原文未覆盖的内容，明确说 "原文未讨论此问题"。
5. 数学公式使用 LaTeX。

{domain_hints}

以下是论文的笔记总结和 Section 索引表，用于定位原文：
---
{notes_summary}
---
Section 索引：
{section_index}
---

输出语言：{language}"""


# ============================================================
# 领域增强注入
# ============================================================

def build_domain_hints(domain_data: Optional[dict]) -> str:
    """构建领域增强 prompt 片段（用于 Stage 1 和研讨模式）"""
    if not domain_data:
        return ""

    parts = [f"该论文属于 {domain_data.get('display_name', '')} 领域，请特别注意："]

    if 'context_hints' in domain_data:
        for hint in domain_data['context_hints']:
            parts.append(f"- {hint}")

    if 'common_techniques' in domain_data:
        parts.append("\n该领域常见的关键技巧（帮助你识别论文中使用的方法）：")
        for tech in domain_data['common_techniques']:
            parts.append(f"- **{tech['name']}**: {tech['description']}")

    if 'standard_notation' in domain_data:
        parts.append("\n标准记号参考：")
        for notation, meaning in domain_data['standard_notation'].items():
            parts.append(f"- ${notation}$: {meaning}")

    return "\n".join(parts)


def build_domain_assumptions(domain_data: Optional[dict]) -> str:
    """构建领域审查要点（用于 Stage 2 批判）"""
    if not domain_data:
        return ""

    parts = ["该领域特别容易遗漏或搞错的假设，请重点检查："]

    if 'critical_assumptions' in domain_data:
        for assumption in domain_data['critical_assumptions']:
            parts.append(f"- {assumption}")

    if 'context_hints' in domain_data:
        parts.append("\n其他注意事项：")
        for hint in domain_data['context_hints']:
            parts.append(f"- {hint}")

    return "\n".join(parts)


def format_prompt(template: str, **kwargs) -> str:
    """格式化 prompt 模板，未提供的变量用空字符串填充"""
    import re
    # 找到所有 {variable} 占位符
    variables = re.findall(r'\{(\w+)\}', template)
    for var in variables:
        if var not in kwargs:
            kwargs[var] = ""
    return template.format(**kwargs)
